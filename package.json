{
  "name": "mern-portfolio",
  "version": "0.0.1",
  "description": "MERN Stack Example Project",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "dev": "concurrently -n 'server,client' -c 'yellow,green' \"nodemon server\" \"npm run client\""
  },
  "author": "JC X",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "concurrently": "^6.0.1",
    "nodemon": "^2.0.7"
  }
}
